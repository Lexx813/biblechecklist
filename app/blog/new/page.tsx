import ClientShell from "../../_components/ClientShell";

// Authoring UI — never indexed.
export const metadata = {
  title: "New Post | JW Study",
  robots: { index: false, follow: false },
};

export default function BlogNewPage() {
  return <ClientShell />;
}
